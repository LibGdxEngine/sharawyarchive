# Sha'rawy Archive — Ralph progress log

## Iteration 1 (2026-08-18)
- Repo was a generic Django+Next.js docker starter (api/core apps, NextAuth frontend). None of the plan implemented.
- Environment: Docker 29 + compose v5, Python 3.12, Node 24, ffmpeg 6.1, network OK (tanzil.net reachable). No GPU, no real Sha'rawy audio.
- Decisions:
  - ASR/align/embed engines pluggable; deterministic stubs for tests; real faster-whisper/e5 modules ship lazily-imported in pipeline/.
  - Filename parser config-driven (parser_patterns.json) since sample filenames unavailable (user offline; plan asked to request 20 samples).
  - R2: endpoint https://6452da3166483560913682a6dd5a5b77.r2.cloudflarestorage.com bucket shaarawy via env keys; MinIO locally with same S3 client code.
- PRD written: 16 stories US-001..US-016 mapping plan phases 0-10.

## Backend core landed (US-002/003/004)
- 70 hand-derived normalization fixtures; harakat strip widened to U+064B-U+0655 (combining maddah/hamza can't NFC-compose over dagger alif/tatweel in Uthmani text — 3204 occurrences); documented in arabic.py docstring.
- Tanzil: outType=txt-2 (not txt) for numbered verses; sura-opening basmala detached from ayah 1 of 112 suras (suras 95/97 use idgham بِّسْمِ — matcher works at normalize_for_index level).
- Data files cached in backend/quran/data/ with ATTRIBUTION.md.
- pgvector HNSW index chunk_embedding_hnsw (m=16, ef_construction=64, cosine) confirmed in pg_indexes.
- Shared modules owned by orchestrator (agents must NOT edit): core/settings/base.py additions, corpus/embeddings.py, corpus/storage.py.
- Concurrent-agent hygiene: per-agent DB_NAME for pytest (pipeline_pg / search_pg), MEILI_INDEX_PREFIX for test isolation.

## Search landed (US-006)
- search.services public API: ensure_chunks_index/index_chunks/delete_segment_chunks/search/rrf_merge/parse_ayah_reference; SearchResponse dataclasses.
- Meili matchingStrategy must be "all": default "last" drops words until the ال prefix matches everything (verified live) — lexical precision, semantic recall.
- HNSW confirmed in EXPLAIN under enable_seqscan=off (chunk_embedding_hnsw index scan).
- mypy baseline: django-stubs plugin not enabled in backend/pyproject.toml (78 pre-existing errors in corpus/quran) — decide in US-016 whether to enable plugin or keep mypy advisory (Makefile already treats mypy as advisory).
